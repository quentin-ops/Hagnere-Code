/**
 * Wrapper around the shared MainNav so the calculator route uses the
 * same dropdown + iconified menu as every other page.
 */
import { MainNav } from "@/components/design-shared/MainNav";

export function CalculatorNav() {
  return <MainNav />;
}
